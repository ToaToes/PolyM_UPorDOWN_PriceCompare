# pip install -r requirements.txt

# WebSocket client for real-time data streaming
websocket-client>=1.8.4

# HTTP requests for API calls
requests>=2.31.0

# For potential future enhancements (optional, not strictly required by current code)
# numpy==1.24.3
